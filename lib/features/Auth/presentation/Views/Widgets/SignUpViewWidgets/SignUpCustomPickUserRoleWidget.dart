import 'package:flutter/material.dart';

class SignUpCustomPickUserRoleWidget extends StatelessWidget {
  const SignUpCustomPickUserRoleWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
